spring:
  application:
    name: api-gateway
  profiles:
    active: test

server:
  port: 8080

logging:
  level:
    root: INFO
    org.springframework.cloud.gateway: INFO
    reactor.netty.http.client: WARN