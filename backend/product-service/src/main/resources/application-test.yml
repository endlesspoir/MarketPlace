spring:
  config:
    activate:
      on-profile: test

  data:
    mongodb:
      uri: mongodb://test:test123@localhost:27017/test_db?authSource=admin


    elasticsearch:
      uris: http://localhost:9200

    redis:
      host: localhost
      port: 6379