spring:
  application:
    name: user-service
    ports: 8081

  profiles:
    active: test

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true


  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    validate-on-migrate: true